import React from 'react';
import {useEffect} from "react";
import {ping} from "../../api/controllers/TestClient";

const Root = () => {
    return (
        <div>
            Root
        </div>
    );
};

export default Root;
